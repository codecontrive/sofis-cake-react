import React from "react";

import { Row } from "react-bootstrap";

import * as styles from "./Logo.module.scss";

const Logo = () => {
  return <Row className={styles.LogoContainer}>da</Row>;
};

export default Logo;
